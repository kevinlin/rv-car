# Renders after the mirror (2026-09-06)

Captured at 1600 x 900 CSS pixels, device pixel ratio 2, from the dev server, at the four stops
that show handedness. Evidence for Task 1 of
[../../specs/plan_correct-against-walkthrough-video.md](../../specs/plan_correct-against-walkthrough-video.md):
the cabin mirrored on X, so the galley and the booth sit on the kerb flank and the washroom pod
and the slide-out on the off flank.

| File | Shows |
|---|---|
| `lounge.png` | Booth kerb, slide-out sofa off — the comparison the plan's *Dissent* section asks for |
| `galley.png` | Counter on the kerb flank, window over it, walnut overheads |
| `washroom.png` | Pod in the rear off corner, opening onto the aisle |
| `exterior.png` | Kerb three-quarter, the flat flank, awaiting Task 6's detail |
| `patch-probe.json` | Raycast sweep used to re-derive `src/calibrate.ts`'s patches |

These predate Tasks 2 to 7, so the palette is still bone-dominant and the exterior is still a
plain white box. `docs/research/final/` gets the re-capture once those land.
